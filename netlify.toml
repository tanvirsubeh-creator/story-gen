[build]
  publish = "."
  functions = "functions"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["node-fetch"]
  timeout = 26  # This gives the AI time to think!

[[redirects]]
  from = "/functions/*"
  to = "/.netlify/functions/:splat"
  status = 200
